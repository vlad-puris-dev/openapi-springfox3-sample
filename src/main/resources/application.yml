server:
  port: 6001

spring:
  datasource:
    name: test
    url: jdbc:h2:mem:mws
    username: test
    password: test
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: "true"
    hibernate.ddl-auto: none